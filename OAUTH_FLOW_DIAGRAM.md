# 🎨 Google OAuth Flow Diagram

## Visual Flow Chart

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                                 │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│  /login page │
└──────┬───────┘
       │
       │ 1. User clicks "Continue with Google"
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Frontend: client/src/pages/Login.tsx                            │
│  ─────────────────────────────────────────────────────────────   │
│  handleGoogleLogin() {                                           │
│    window.location.href = '/api/auth/google/redirect'           │
│  }                                                               │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ 2. Redirect to backend
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Backend: api/auth/google/redirect.ts                            │
│  ─────────────────────────────────────────────────────────────   │
│  • Generate random state (CSRF protection)                       │
│  • Build Google OAuth URL                                        │
│  • Set secure cookie with state                                 │
│  • Return 302 redirect to Google                                │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ 3. 302 Redirect
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Google OAuth Server                                             │
│  ─────────────────────────────────────────────────────────────   │
│  https://accounts.google.com/o/oauth2/v2/auth                    │
│                                                                  │
│  User sees Google login page                                    │
│  User enters credentials                                        │
│  User grants permissions                                        │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ 4. Google redirects back with code
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Backend: api/auth/google/callback.ts                            │
│  ─────────────────────────────────────────────────────────────   │
│  GET /api/auth/google/callback?code=xxx&state=yyy                │
│                                                                  │
│  Step 1: Validate state parameter                               │
│  ├─ Read state from cookie                                      │
│  ├─ Compare with URL state parameter                            │
│  └─ Reject if mismatch (CSRF protection)                        │
│                                                                  │
│  Step 2: Exchange code for tokens                               │
│  ├─ POST to https://oauth2.googleapis.com/token                 │
│  ├─ Send: code, client_id, client_secret, redirect_uri          │
│  └─ Receive: access_token, id_token, refresh_token              │
│                                                                  │
│  Step 3: Fetch user profile                                     │
│  ├─ GET https://www.googleapis.com/oauth2/v2/userinfo           │
│  ├─ Send: Authorization: Bearer {access_token}                  │
│  └─ Receive: { id, email, name, picture }                       │
│                                                                  │
│  Step 4: Database operations (Drizzle ORM)                       │
│  ├─ Check if user exists: db.select().where(eq(email))          │
│  ├─ If exists:                                                  │
│  │   └─ Update: googleId, avatarUrl, updatedAt                  │
│  └─ If new:                                                     │
│      └─ Insert: email, fullName, googleId, avatarUrl, password=null│
│                                                                  │
│  Step 5: Generate JWT token                                     │
│  ├─ jwt.sign({ id, email, googleId, name })                     │
│  ├─ Secret: process.env.JWT_SECRET                              │
│  └─ Expiry: 7 days                                              │
│                                                                  │
│  Step 6: Redirect to frontend with token                        │
│  └─ 302 to /login?token={JWT}                                   │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ 5. Redirect with token
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Frontend: client/src/pages/Login.tsx                            │
│  ─────────────────────────────────────────────────────────────   │
│  useEffect(() => {                                               │
│    const token = searchParams.get('token')                       │
│    if (token) {                                                  │
│      localStorage.setItem('authToken', token)                    │
│      localStorage.setItem('isLoggedIn', 'true')                  │
│      navigate('/dashboard')                                      │
│    }                                                             │
│  }, [searchParams])                                              │
└──────────────────────────┬───────────────────────────────────────┘
                           │
                           │ 6. Navigate to dashboard
                           │
                           ▼
┌──────────────┐
│   Browser    │
│  /dashboard  │
│  ✅ Logged in│
└──────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│  SECURITY FEATURES                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. CSRF Protection                                              │
│     ├─ Random state parameter generated                         │
│     ├─ Stored in httpOnly cookie                                │
│     └─ Validated on callback                                    │
│                                                                  │
│  2. Secure Cookies                                               │
│     ├─ httpOnly: true (no JavaScript access)                    │
│     ├─ secure: true (HTTPS only)                                │
│     ├─ sameSite: 'lax' (CSRF protection)                        │
│     └─ maxAge: 600 (10 minutes)                                 │
│                                                                  │
│  3. JWT Tokens                                                   │
│     ├─ Signed with JWT_SECRET                                   │
│     ├─ 7-day expiration                                         │
│     └─ Contains: id, email, googleId, name                      │
│                                                                  │
│  4. Environment Variables                                        │
│     ├─ All secrets in process.env                               │
│     ├─ Never committed to git                                   │
│     └─ Managed in Vercel dashboard                              │
│                                                                  │
│  5. Database Security                                            │
│     ├─ Unique constraints on email and googleId                 │
│     ├─ Password nullable for OAuth users                        │
│     └─ Drizzle ORM prevents SQL injection                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Database Schema Changes

```
┌─────────────────────────────────────────────────────────────────┐
│  BEFORE (Original Schema)                                        │
├─────────────────────────────────────────────────────────────────┤
│  users table:                                                    │
│  ├─ id: uuid (PK)                                                │
│  ├─ fullName: text                                               │
│  ├─ email: text (unique)                                         │
│  ├─ password: text                                               │
│  ├─ createdAt: timestamp                                         │
│  └─ updatedAt: timestamp                                         │
└─────────────────────────────────────────────────────────────────┘
                           │
                           │ npm run db:push
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  AFTER (Updated Schema)                                          │
├─────────────────────────────────────────────────────────────────┤
│  users table:                                                    │
│  ├─ id: uuid (PK)                                                │
│  ├─ fullName: text                                               │
│  ├─ email: text (unique)                                         │
│  ├─ password: text (nullable) ◄── Changed to nullable            │
│  ├─ googleId: text (unique, nullable) ◄── NEW                    │
│  ├─ avatarUrl: text (nullable) ◄── NEW                           │
│  ├─ createdAt: timestamp                                         │
│  └─ updatedAt: timestamp                                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## User Types

```
┌─────────────────────────────────────────────────────────────────┐
│  EMAIL/PASSWORD USER                                             │
├─────────────────────────────────────────────────────────────────┤
│  {                                                               │
│    id: "uuid-123",                                               │
│    fullName: "John Doe",                                         │
│    email: "john@example.com",                                    │
│    password: "$2a$10$hashed...",  ◄── Has password               │
│    googleId: null,                 ◄── No Google ID              │
│    avatarUrl: null,                ◄── No avatar                 │
│    createdAt: "2025-11-29",                                      │
│    updatedAt: "2025-11-29"                                       │
│  }                                                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  GOOGLE OAUTH USER                                               │
├─────────────────────────────────────────────────────────────────┤
│  {                                                               │
│    id: "uuid-456",                                               │
│    fullName: "Jane Smith",                                       │
│    email: "jane@gmail.com",                                      │
│    password: null,                 ◄── No password               │
│    googleId: "1234567890",         ◄── Has Google ID             │
│    avatarUrl: "https://...",       ◄── Has avatar                │
│    createdAt: "2025-11-29",                                      │
│    updatedAt: "2025-11-29"                                       │
│  }                                                               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  HYBRID USER (Started with email, later linked Google)          │
├─────────────────────────────────────────────────────────────────┤
│  {                                                               │
│    id: "uuid-789",                                               │
│    fullName: "Bob Johnson",                                      │
│    email: "bob@example.com",                                     │
│    password: "$2a$10$hashed...",  ◄── Has password               │
│    googleId: "9876543210",         ◄── Has Google ID             │
│    avatarUrl: "https://...",       ◄── Has avatar                │
│    createdAt: "2025-11-20",                                      │
│    updatedAt: "2025-11-29"         ◄── Updated when linked       │
│  }                                                               │
│                                                                  │
│  Can login with BOTH email/password AND Google OAuth!           │
└─────────────────────────────────────────────────────────────────┘
```

---

## API Endpoints

```
┌─────────────────────────────────────────────────────────────────┐
│  AUTHENTICATION ENDPOINTS                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  POST /api/auth/register                                         │
│  ├─ Body: { email, password, fullName }                         │
│  └─ Returns: { token }                                          │
│                                                                  │
│  POST /api/auth/login                                            │
│  ├─ Body: { email, password }                                   │
│  └─ Returns: { token }                                          │
│                                                                  │
│  GET /api/auth/google/redirect ◄── NEW                           │
│  ├─ Generates state                                             │
│  ├─ Sets cookie                                                 │
│  └─ Redirects to Google                                         │
│                                                                  │
│  GET /api/auth/google/callback ◄── NEW                           │
│  ├─ Query: ?code=xxx&state=yyy                                  │
│  ├─ Validates state                                             │
│  ├─ Exchanges code for tokens                                   │
│  ├─ Creates/updates user                                        │
│  ├─ Generates JWT                                               │
│  └─ Redirects to /login?token=JWT                               │
│                                                                  │
│  GET /api/auth/me                                                │
│  ├─ Header: Authorization: Bearer {token}                       │
│  └─ Returns: { user }                                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Environment Variables Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  DEVELOPMENT (.env file)                                         │
├─────────────────────────────────────────────────────────────────┤
│  DATABASE_URL=postgresql://...                                   │
│  JWT_SECRET=dev_secret_key_123                                   │
│  GOOGLE_CLIENT_ID=123456.apps.googleusercontent.com              │
│  GOOGLE_CLIENT_SECRET=GOCSPX-abc123                              │
│  GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback│
└─────────────────────────────────────────────────────────────────┘
                           │
                           │ git push (env file not committed)
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  PRODUCTION (Vercel Dashboard)                                   │
├─────────────────────────────────────────────────────────────────┤
│  Settings → Environment Variables                                │
│                                                                  │
│  DATABASE_URL=postgresql://...                                   │
│  JWT_SECRET=prod_secret_key_xyz                                  │
│  GOOGLE_CLIENT_ID=123456.apps.googleusercontent.com              │
│  GOOGLE_CLIENT_SECRET=GOCSPX-abc123                              │
│  GOOGLE_REDIRECT_URI=https://tenderlert.vercel.app/api/auth/google/callback│
└─────────────────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  ERROR SCENARIOS                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1. Missing Environment Variables                                │
│     ├─ Error: "Failed to initiate Google OAuth"                 │
│     ├─ Status: 500                                              │
│     └─ Fix: Add vars to Vercel                                  │
│                                                                  │
│  2. State Mismatch (CSRF Attack)                                 │
│     ├─ Error: "State mismatch - possible CSRF attack"           │
│     ├─ Status: 403                                              │
│     └─ Fix: Clear cookies, try again                            │
│                                                                  │
│  3. Invalid Authorization Code                                   │
│     ├─ Error: "Failed to exchange code for tokens"              │
│     ├─ Status: 500                                              │
│     └─ Fix: Check Google Console settings                       │
│                                                                  │
│  4. Database Connection Error                                    │
│     ├─ Error: "Server error"                                    │
│     ├─ Status: 500                                              │
│     └─ Fix: Check DATABASE_URL                                  │
│                                                                  │
│  5. User Denies Permissions                                      │
│     ├─ Google redirects with error parameter                    │
│     ├─ Frontend shows: "Authentication failed"                  │
│     └─ User can try again                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

This diagram provides a complete visual reference for understanding
the Google OAuth 2.0 integration in TenderLert!
